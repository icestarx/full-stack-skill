interface:
  display_name: "Full-Stack Delivery"
  short_description: "Ship multi-layer changes through four evidence tracks"
  default_prompt: "Use $full-stack-skill to route, implement, verify, and deliver this multi-layer change through its four-track workflow."
policy:
  allow_implicit_invocation: true
